slam_toolbox:
  ros__parameters:
    # --- Frames / topics (match the JetAuto bring-up) ---
    use_sim_time: false
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan
    mode: mapping                 # mapping | localization

    # --- Map ---
    resolution: 0.05
    map_update_interval: 2.0
    transform_publish_period: 0.02
    map_start_pose: [0.0, 0.0, 0.0]

    # --- Lidar range (Orbbec MS200; spec 0.03-12 m) ---
    # Caracterizado 2026-06-04 (10 h quieto, ver tools/characterization): sigma_range ~4.3 mm.
    # La escena no tuvo blancos >~5 m -> max confiable confirmado solo a ~5 m; 8.0 = margen seguro
    # (mas alla, retornos lejanos con sigma alta meten ruido al scan-match). min 0.05: el MS200 ve
    # a 3 cm (antes 0.15, era del A1). OJO: verificar en RViz que no haya auto-retornos del chasis
    # (lidar 9.6 cm adelantado); si los hay cerca, subir min a 0.10.
    min_laser_range: 0.05
    max_laser_range: 8.0

    # --- Scan matching / graph (slam_toolbox defaults, tuned light) ---
    debug_logging: false
    throttle_scans: 1
    transform_timeout: 0.2
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000
    enable_interactive_mode: true

    # EVO ronda 1 (Grupo A, frecuencia de scan-match): bajados de 0.2 -> procesa scans 2x mas
    # seguido = menos drift acumulado. Baseline en mapper_params_online_async.yaml.deadreckoning.bak.
    minimum_travel_distance: 0.15   # baseline 0.2
    minimum_travel_heading: 0.15    # baseline 0.2
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
